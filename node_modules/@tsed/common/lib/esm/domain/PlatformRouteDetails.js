import { ParamTypes } from "@tsed/platform-params";
import { JsonOperationRoute } from "@tsed/schema";
export class PlatformRouteDetails extends JsonOperationRoute {
    constructor(options = {}) {
        super(options);
        this.rawBody = !!this.endpoint.parameters.find((param) => param.paramType === ParamTypes.RAW_BODY);
    }
    toJSON() {
        return {
            method: this.method,
            name: this.name,
            url: this.url,
            className: this.className,
            methodClassName: this.methodClassName,
            parameters: this.parameters,
            rawBody: this.rawBody
        };
    }
}
//# sourceMappingURL=PlatformRouteDetails.js.map