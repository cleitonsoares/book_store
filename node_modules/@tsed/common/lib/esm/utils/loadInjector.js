import { createContainer } from "@tsed/di";
/**
 * @ignore
 * @deprecated User injector.load(container, module) instead
 */
export async function loadInjector(injector, container = createContainer()) {
    injector.bootstrap(container);
    return injector.load(container);
}
//# sourceMappingURL=loadInjector.js.map