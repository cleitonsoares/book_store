import { PlatformHandler } from "./PlatformHandler";
import { PlatformRouter } from "./PlatformRouter";
import { PlatformAdapter } from "./PlatformAdapter";
declare global {
    namespace TsED {
        interface Application {
        }
    }
}
/**
 * `PlatformApplication` is used to provide all routes collected by annotation `@Controller`.
 *
 * @platform
 */
export declare class PlatformApplication<App = TsED.Application, Router = TsED.Router> extends PlatformRouter<App, Router> {
    raw: App;
    rawApp: App;
    rawRouter: Router;
    constructor(platformHandler: PlatformHandler, adapter: PlatformAdapter<App, Router>);
    getApp(): App;
}
