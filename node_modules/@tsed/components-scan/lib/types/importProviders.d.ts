/**
 * Lookup settings and replace glob pattern by the resolved providers
 *
 * @param settings
 */
export declare function importProviders(settings: Partial<TsED.Configuration>): Promise<Partial<TsED.Configuration>>;
