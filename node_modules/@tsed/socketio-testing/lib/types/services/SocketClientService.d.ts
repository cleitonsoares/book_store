import { OnDestroy } from "@tsed/common";
import { Socket } from "socket.io-client";
export declare class SocketClientService implements OnDestroy {
    private settings;
    private logger;
    private clients;
    get(namespace?: string, path?: string): Promise<Socket>;
    $onDestroy(): void;
}
